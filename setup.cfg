[tool:pytest]
addopts = -v -s
          --doctest-modules
          --pep8
          --cov=iotlabwebtools --cov-report=term --cov-report=term-missing
testpaths = iotlabwebtools

[lint]
lint-reports = no
lint-disable = locally-disabled,star-args,bad-option-value
lint-msg-template = {path}:{line}: [{msg_id}({symbol}), {obj}] {msg}

[pep8]
exclude = *.egg,.tox
